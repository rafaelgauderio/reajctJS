import React, {Fragment} from 'react';
import ReactDOM from "react-dom";
import "./styles.css";

const element = 'Rafael De Luca';
const element2 = 'Mensagem teste';

function App() {

	return (
		<div>
			{element}
			{element2}
		<div>
		)
	}

	const rootElement = document.getElementById("root")
	ReactDOM.render(<App />), rootElement)

